# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'tawk.to exam' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for tawk.to exam
  pod 'SDWebImage'
  pod 'SVProgressHUD'
  # Keyboard
  pod 'IQKeyboardManagerSwift'

  target 'tawk.to examTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'tawk.to examUITests' do
    # Pods for testing
  end

end
